@import 'course.wxss';
@import 'table.wxss';
@import 'navbar.wxss';
@import 'layer.wxss';

modal input {
  border: 1rpx solid rgba(0,0,0,.3);
  padding: 5px;
  color: #000;
  line-height: 1.5;
}

modal input.first {
  margin-top: 20rpx;
  border-bottom: none;
}

.blur {
  filter: blur(4px);
}